{
  "name": "loupe",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "desktop:dev": "pnpm --filter desktop dev",
    "desktop:build": "pnpm --filter desktop build",
    "desktop:test": "pnpm --filter desktop test",
    "vendor:prepare:mac": "bash scripts/prepare-vendor-binaries.sh --ci --with-uxplay",
    "vendor:prepare:win": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/prepare-vendor-binaries.ps1 -Ci",
    "rebuild:electron": "node scripts/rebuild-electron.mjs",
    "rebuild:node": "pnpm --filter desktop rebuild better-sqlite3"
  }
}
